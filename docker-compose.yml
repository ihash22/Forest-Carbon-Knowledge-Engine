version: '3.8'

services:
  carbon-knowledge-api:
    build: .
    container_name: forest_carbon_api
    ports:
      - "8000:8000"
    env_file:
      - .env  # This securely injects your OpenAI API key into the container
    restart: unless-stopped
